package com.example.parcial

import androidx.appcompat.app.AppCompatActivity
import android.os.Bundle
//en este activity solo se utilizo el recurso background para mostrar la imagen donde aparece la informacion del estudiante
//cabe aclarar que todos estos recursos se realizaron en la plataforma canva
class creador : AppCompatActivity() {
    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
        setContentView(R.layout.activity_creador)
    }
}